---
import { Image } from 'astro:assets';
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import profileImg from '../assets/profile.png';

const allPosts = await getCollection('blog', ({ data }) => !data.draft);
const posts = allPosts.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const categories = [
  { slug: 'tumu', label: 'Tümü' },
  { slug: 'teknoloji', label: 'Teknoloji' },
  { slug: 'bilim', label: 'Bilim' },
  { slug: 'hayat', label: 'Hayat' },
  { slug: 'notlar', label: 'Notlar' },
];

const featuredPosts = posts.slice(0, 3);

function formatDate(date: Date): string {
  return date.toLocaleDateString('tr-TR', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
  });
}
---

<BaseLayout title="Semih Mutlu" description="Okur, yazar, bozar, yapar. Teknoloji, bilim ve hayat üzerine yazılar.">

  <!-- Hero Section -->
  <section class="py-10 md:py-14 border-b border-[#e0e0e0]">
    <div class="flex flex-col sm:flex-row items-center sm:items-start gap-8">
      <!-- Profile photo -->
      <Image
        src={profileImg}
        alt="Semih Mutlu"
        width={180}
        height={180}
        class="w-[180px] h-[180px] object-cover flex-shrink-0"
      />

      <div class="text-center sm:text-left">
        <h1 class="font-serif text-3xl md:text-4xl font-bold tracking-tight text-[#1a1a1a]">
          Semih Mutlu
        </h1>
        <p class="mt-2 text-[#666] italic">
          okur, yazar, bozar, yapar.
        </p>
        <p class="mt-3 text-[#444] leading-relaxed max-w-lg">
          Teknoloji, bilim ve hayat üzerine düşüncelerimi paylaştığım kişisel blog.
          Yazılım geliştirici, meraklı okur, ara sıra filmci.
        </p>

      </div>
    </div>
  </section>

  <!-- Category Bar -->
  <nav class="py-4 border-b border-[#e0e0e0] flex items-center gap-2 overflow-x-auto">
    {categories.map(({ slug, label }) => (
      <button
        data-category={slug}
        class:list={[
          'category-btn px-4 py-1.5 text-sm whitespace-nowrap transition-colors',
          slug === 'tumu'
            ? 'bg-[#1a1a1a] text-white'
            : 'bg-[#f0f0f0] text-[#666] hover:bg-[#e0e0e0] hover:text-[#1a1a1a]',
        ]}
      >
        {label}
      </button>
    ))}
  </nav>

  <!-- Two Column Layout -->
  <div class="grid grid-cols-1 lg:grid-cols-3 gap-10 py-8 md:py-12">

    <!-- Main: Post List -->
    <div class="lg:col-span-2">
      <h2 class="font-serif text-xl font-bold text-[#1a1a1a] mb-6">Son Yazılar</h2>

      {posts.length === 0 && (
        <p class="text-[#999]">Henüz yazı yok.</p>
      )}

      <div id="post-list" class="flex flex-col divide-y divide-[#e0e0e0]">
        {posts.map((post) => (
          <article
            class="post-item py-6 first:pt-0"
            data-tags={post.data.tags.join(',')}
          >
            <div class="flex items-center gap-3 mb-2">
              <time class="text-sm text-[#999]" datetime={post.data.date.toISOString()}>
                {formatDate(post.data.date)}
              </time>
              {post.data.tags.slice(0, 2).map((tag) => (
                <a
                  href={`/blog/tags/${tag}`}
                  class="text-xs px-2 py-0.5 bg-[#f0f0f0] text-[#666] hover:bg-[#e0e0e0] transition-colors"
                >
                  {tag}
                </a>
              ))}
            </div>
            <h3 class="font-serif text-lg md:text-xl font-bold text-[#1a1a1a] hover:text-[#444] transition-colors">
              <a href={`/blog/${post.id}`}>
                {post.data.title}
              </a>
            </h3>
            <p class="mt-2 text-[#666] text-sm leading-relaxed line-clamp-2">
              {post.data.description}
            </p>
          </article>
        ))}
      </div>
    </div>

    <!-- Sidebar -->
    <aside class="space-y-8">

      <!-- Hakkımda -->
      <div class="border border-[#e0e0e0] p-5">
        <h3 class="font-serif text-base font-bold text-[#1a1a1a] mb-3">Hakkımda</h3>
        <p class="text-sm text-[#666] leading-relaxed">
          Yazılım geliştirici, meraklı okur, filmci. Teknoloji ve bilim arasında
          köprü kurmaya çalışıyor, arada hayata dair notlar düşüyorum.
        </p>
        <a href="/about" class="inline-block mt-3 text-sm text-[#1a1a1a] font-medium hover:text-[#666] transition-colors">
          Devamını oku &rarr;
        </a>
      </div>

      <!-- Öne Çıkanlar -->
      <div class="border border-[#e0e0e0] p-5">
        <h3 class="font-serif text-base font-bold text-[#1a1a1a] mb-3">Öne Çıkanlar</h3>
        <ul class="space-y-2">
          {featuredPosts.map((post) => (
            <li>
              <a
                href={`/blog/${post.id}`}
                class="text-sm text-[#666] hover:text-[#1a1a1a] transition-colors leading-snug block"
              >
                {post.data.title}
              </a>
            </li>
          ))}
        </ul>
      </div>

    </aside>
  </div>

</BaseLayout>

<script>
  const buttons = document.querySelectorAll<HTMLButtonElement>('.category-btn');
  const posts = document.querySelectorAll<HTMLElement>('.post-item');

  buttons.forEach((btn) => {
    btn.addEventListener('click', () => {
      const category = btn.dataset.category;

      // Update active button style
      buttons.forEach((b) => {
        b.classList.remove('bg-[#1a1a1a]', 'text-white');
        b.classList.add('bg-[#f0f0f0]', 'text-[#666]');
      });
      btn.classList.remove('bg-[#f0f0f0]', 'text-[#666]');
      btn.classList.add('bg-[#1a1a1a]', 'text-white');

      // Filter posts
      posts.forEach((post) => {
        const tags = post.dataset.tags?.split(',') ?? [];
        if (category === 'tumu' || tags.includes(category ?? '')) {
          post.style.display = '';
        } else {
          post.style.display = 'none';
        }
      });
    });
  });
</script>
