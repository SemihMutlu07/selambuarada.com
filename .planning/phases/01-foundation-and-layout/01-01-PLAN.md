---
phase: 01-foundation-and-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - tsconfig.json
  - src/styles/global.css
  - src/content.config.ts
  - src/content/projects.json
  - src/content/films.json
  - src/content/photosets.json
  - src/pages/index.astro
  - src/env.d.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05

must_haves:
  truths:
    - "npm run build completes without errors"
    - "TypeScript strict mode is enforced"
    - "Tailwind v4 utilities resolve (dark variant declared)"
    - "MDX integration is registered"
    - "GSAP is installed in node_modules"
    - "Geist font package is installed"
    - "All four content collections are defined with typed Zod schemas"
    - "Content collections produce zero schema errors at build time"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with all dependencies"
      contains: "astro"
    - path: "astro.config.mjs"
      provides: "Astro config with MDX + Tailwind v4 vite plugin"
      contains: "tailwindcss"
    - path: "tsconfig.json"
      provides: "TypeScript strict config"
      contains: "astro/tsconfigs/strict"
    - path: "src/styles/global.css"
      provides: "Tailwind v4 CSS config with dark mode variant and Geist font token"
      contains: "@custom-variant dark"
    - path: "src/content.config.ts"
      provides: "All four content collection schemas"
      exports: ["collections"]
    - path: "src/content/projects.json"
      provides: "Empty projects collection data"
    - path: "src/content/films.json"
      provides: "Empty films collection data"
    - path: "src/content/photosets.json"
      provides: "Empty photosets collection data"
  key_links:
    - from: "astro.config.mjs"
      to: "@tailwindcss/vite"
      via: "vite.plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "astro.config.mjs"
      to: "@astrojs/mdx"
      via: "integrations array"
      pattern: "mdx\\(\\)"
    - from: "src/content.config.ts"
      to: "astro/loaders"
      via: "glob and file imports"
      pattern: "from 'astro/loaders'"
    - from: "src/content.config.ts"
      to: "astro/zod"
      via: "z import"
      pattern: "from 'astro/zod'"
    - from: "src/styles/global.css"
      to: "tailwindcss"
      via: "@import directive"
      pattern: '@import "tailwindcss"'
---

<objective>
Scaffold the Astro 5 project from scratch, install all dependencies, configure Tailwind v4 via the Vite plugin, set up MDX integration, and define all four content collection schemas with typed Zod schemas.

Purpose: This is the technical foundation everything else builds on. Getting the scaffold, tooling, and content schemas right in one shot prevents cascading issues in later phases.
Output: A buildable Astro 5 project with TypeScript strict, Tailwind v4, MDX, GSAP, Geist font, and four typed content collections.
</objective>

<execution_context>
@/home/parkermutsuz/.claude/get-shit-done/workflows/execute-plan.md
@/home/parkermutsuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-layout/01-RESEARCH.md
@.planning/phases/01-foundation-and-layout/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro 5 project with all dependencies and tooling config</name>
  <files>package.json, astro.config.mjs, tsconfig.json, src/styles/global.css, src/pages/index.astro, src/env.d.ts</files>
  <action>
1. Scaffold the project using `npm create astro@latest -- . --template minimal --typescript strict --install --no-git` (the `.` uses the current directory since the git repo already exists).

2. Add MDX integration: `npx astro add mdx --yes`

3. Install Tailwind v4, font, and GSAP: `npm install tailwindcss @tailwindcss/vite @fontsource-variable/geist gsap`

4. Update `astro.config.mjs` to add the Tailwind v4 Vite plugin. The final file should look like:
```javascript
import { defineConfig } from 'astro/config';
import mdx from '@astrojs/mdx';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  integrations: [mdx()],
  vite: {
    plugins: [tailwindcss()],
  },
});
```

5. Ensure `tsconfig.json` extends `astro/tsconfigs/strict`:
```json
{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"]
}
```

6. Create `src/styles/global.css` with Tailwind v4 CSS-native config, dark mode variant, and Geist font token:
```css
@import "tailwindcss";

@custom-variant dark (&:where(.dark, .dark *));

@theme inline {
  --font-sans: 'Geist Variable', ui-sans-serif, system-ui, sans-serif;
}
```

7. Update the default `src/pages/index.astro` to a minimal placeholder that imports global.css (just enough to verify the build works):
```astro
---
import '../styles/global.css';
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Parker Mutsuz</title>
  </head>
  <body class="font-sans">
    <h1>Site scaffold working</h1>
  </body>
</html>
```

CRITICAL anti-patterns to avoid (from research):
- Do NOT create `tailwind.config.js` — Tailwind v4 uses CSS-only config
- Do NOT install `@astrojs/tailwind` — that is the old v3 integration
- Do NOT install bare `zod` — use `astro/zod` which is bundled
- Use `@custom-variant dark` NOT `@variant dark`
  </action>
  <verify>
    <automated>cd /home/parkermutsuz/dev/blog && npm run build</automated>
  </verify>
  <done>Build passes clean. package.json contains astro, @astrojs/mdx, tailwindcss, @tailwindcss/vite, @fontsource-variable/geist, and gsap as dependencies. astro.config.mjs has both MDX integration and Tailwind vite plugin. tsconfig.json extends astro/tsconfigs/strict. global.css has @import "tailwindcss", @custom-variant dark, and @theme inline with Geist font.</done>
</task>

<task type="auto">
  <name>Task 2: Define all four content collection schemas and create empty content data files</name>
  <files>src/content.config.ts, src/content/projects.json, src/content/films.json, src/content/photosets.json</files>
  <action>
1. Create `src/content.config.ts` at the src root (NOT inside src/content/) with all four collections:

```typescript
import { defineCollection } from 'astro:content';
import { glob, file } from 'astro/loaders';
import { z } from 'astro/zod';

const blog = defineCollection({
  loader: glob({ pattern: '**/*.mdx', base: './src/content/blog' }),
  schema: z.object({
    title: z.string(),
    date: z.coerce.date(),
    description: z.string(),
    tags: z.array(z.string()).default([]),
  }),
});

const projects = defineCollection({
  loader: file('./src/content/projects.json'),
  schema: z.object({
    title: z.string(),
    description: z.string(),
    tech: z.array(z.string()),
    url: z.string().url().optional(),
    github: z.string().url().optional(),
  }),
});

const films = defineCollection({
  loader: file('./src/content/films.json'),
  schema: z.object({
    title: z.string(),
    year: z.number(),
    role: z.string(),
    embedUrl: z.string().url(),
    description: z.string(),
    credits: z.array(z.object({
      role: z.string(),
      name: z.string(),
    })),
  }),
});

const photosets = defineCollection({
  loader: file('./src/content/photosets.json'),
  schema: z.object({
    title: z.string(),
    description: z.string(),
    images: z.array(z.object({
      src: z.string(),
      alt: z.string(),
    })),
  }),
});

export const collections = { blog, projects, films, photosets };
```

2. Create `src/content/blog/` directory (empty — no placeholder MDX files, per "no filler" constraint).

3. Create `src/content/projects.json` as an empty JSON array: `[]`

4. Create `src/content/films.json` as an empty JSON array: `[]`

5. Create `src/content/photosets.json` as an empty JSON array: `[]`

CRITICAL anti-patterns to avoid:
- Do NOT place this file at `src/content/config.ts` — Astro 5 requires `src/content.config.ts`
- Do NOT use `type: 'content'` or `type: 'data'` — replaced by `loader` property in Astro 5
- Import `z` from `astro/zod`, NOT from bare `zod`
- Import `glob`, `file` from `astro/loaders` (no colon), NOT `astro:loaders`
  </action>
  <verify>
    <automated>cd /home/parkermutsuz/dev/blog && npm run build && npx astro check</automated>
  </verify>
  <done>Build passes with all four content collections registered. `npx astro check` reports no TypeScript errors. The schemas define: blog (title, date, description, tags), projects (title, description, tech, url, github), films (title, year, role, embedUrl, description, credits), photosets (title, description, images). Empty JSON files exist for projects, films, and photosets. Blog directory exists but is empty.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with exit code 0
2. `npx astro check` reports no TypeScript errors
3. `ls node_modules/gsap` confirms GSAP is installed
4. `ls node_modules/@fontsource-variable/geist` confirms Geist font is installed
5. `grep -r "@custom-variant dark" src/styles/global.css` confirms dark mode variant
6. `grep -r "export const collections" src/content.config.ts` confirms collections export
</verification>

<success_criteria>
- Clean build with zero errors
- TypeScript strict mode active
- All four content collection schemas defined and typed
- Tailwind v4 configured via Vite plugin with dark mode variant and Geist font token
- MDX integration registered
- GSAP and Geist font packages present in node_modules
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-layout/01-01-SUMMARY.md`
</output>
