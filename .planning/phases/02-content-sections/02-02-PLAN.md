---
phase: 02-content-sections
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/projects/index.astro
  - src/content/projects.json
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-03]

must_haves:
  truths:
    - "Visiting /projects shows project cards with title, description, tech stack, and links"
    - "Project data is validated by the Zod schema in content.config.ts"
    - "Each project card shows tech stack as individual tags/badges"
    - "External links (url, github) render as clickable links when present"
  artifacts:
    - path: "src/pages/projects/index.astro"
      provides: "Projects index page with card layout"
      contains: "getCollection"
    - path: "src/content/projects.json"
      provides: "Seed project data for build verification"
      contains: "title"
  key_links:
    - from: "src/pages/projects/index.astro"
      to: "astro:content"
      via: "getCollection('projects')"
      pattern: "getCollection\\('projects'\\)"
    - from: "src/pages/projects/index.astro"
      to: "src/content/projects.json"
      via: "content collection data"
      pattern: "project\\.data\\."
---

<objective>
Build the projects section: a single index page displaying project cards from the projects content collection.

Purpose: Projects is the simplest content section — a single page with cards. No dynamic routes needed. The existing schema already covers all data fields (title, description, tech, url, github).
Output: Fully functional /projects page with seed data and card layout.
</objective>

<execution_context>
@/home/parkermutsuz/.claude/get-shit-done/workflows/execute-plan.md
@/home/parkermutsuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/content.config.ts — projects collection schema:
```typescript
const projects = defineCollection({
  loader: file('./src/content/projects.json'),
  schema: z.object({
    title: z.string(),
    description: z.string(),
    tech: z.array(z.string()),
    url: z.string().url().optional(),
    github: z.string().url().optional(),
  }),
});
```

From src/layouts/BaseLayout.astro — Props interface:
```typescript
interface Props {
  title: string;
  description?: string;
  ogImage?: string;  // Added by 02-01 or may not exist yet — use title+description only
}
```

From src/pages/projects/index.astro — current stub:
```astro
<BaseLayout title="Projects">
  <h1 class="text-5xl md:text-7xl font-bold tracking-tight">Projects</h1>
  <p class="mt-4 text-lg text-neutral-600 dark:text-neutral-400">Software I've built and shipped.</p>
</BaseLayout>
```

Patterns:
- Brutalist aesthetic: text-5xl md:text-7xl font-bold tracking-tight headings, no rounded corners, high contrast
- Dark mode: dark: variant classes throughout
- BaseLayout wraps every page with title prop
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed projects.json with sample data</name>
  <files>src/content/projects.json</files>
  <action>
Replace the empty array in `src/content/projects.json` with 2 sample project entries that exercise all schema fields. Each entry MUST have an `id` field (required by Astro's file() loader for JSON arrays):

```json
[
  {
    "id": "sample-project-1",
    "title": "CLI Tool",
    "description": "A command-line utility for batch processing files. Built to learn Rust's ownership model.",
    "tech": ["Rust", "Clap", "Serde"],
    "url": "https://example.com/cli-tool",
    "github": "https://github.com/example/cli-tool"
  },
  {
    "id": "sample-project-2",
    "title": "Photo Gallery",
    "description": "A static photo gallery generator with automatic image optimization.",
    "tech": ["TypeScript", "Astro", "Sharp"],
    "github": "https://github.com/example/photo-gallery"
  }
]
```

The second entry intentionally omits `url` to verify the optional field works. Both entries exist purely for build verification and route testing — they will be replaced with real content before shipping.
  </action>
  <verify>
    <automated>cd /home/parkermutsuz/dev/blog && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>projects.json contains 2 valid entries. Zod schema validation passes at build time. No build errors.</done>
</task>

<task type="auto">
  <name>Task 2: Projects index page with card layout</name>
  <files>src/pages/projects/index.astro</files>
  <action>
Replace `src/pages/projects/index.astro` stub with the full projects page:

1. Import `getCollection` from `astro:content` and `BaseLayout`
2. Fetch projects: `const projects = await getCollection('projects');`
3. Render with brutalist card layout:
   - Keep the `<h1>` heading: "Projects" with `text-5xl md:text-7xl font-bold tracking-tight`
   - Subtitle: "Software I've built and shipped." in `text-lg text-neutral-600 dark:text-neutral-400`
   - Below, a grid of project cards: `grid grid-cols-1 md:grid-cols-2 gap-8 mt-12`
   - Each card is a `<div>` with brutalist styling:
     - Border: `border-2 border-neutral-900 dark:border-neutral-100` (harsh, no radius — already enforced globally)
     - Padding: `p-6`
     - Title: `<h2>` with `text-2xl font-bold`
     - Description: `<p>` with `mt-2 text-neutral-600 dark:text-neutral-400`
     - Tech stack: a flex-wrap row of small tags/badges below description
       - Each tech item: `<span>` with `text-xs font-bold uppercase tracking-wider border border-neutral-400 dark:border-neutral-600 px-2 py-1`
       - Wrap in `<div class="flex flex-wrap gap-2 mt-4">`
     - Links section at bottom: `<div class="flex gap-4 mt-4">`
       - If `project.data.url` exists: `<a href={url} target="_blank" rel="noopener">Visit</a>` styled as text-sm font-bold uppercase underline
       - If `project.data.github` exists: `<a href={github} target="_blank" rel="noopener">GitHub</a>` styled same
   - Handle empty state: if no projects, show "No projects yet."
4. Pass title="Projects" and description="Software I've built and shipped." to BaseLayout
  </action>
  <verify>
    <automated>cd /home/parkermutsuz/dev/blog && npm run build 2>&1 | tail -10 && test -f dist/projects/index.html && echo "PASS: projects page generated"</automated>
  </verify>
  <done>Projects page at /projects displays 2 project cards. Each card shows title, description, tech badges, and relevant links. Cards use brutalist styling with hard borders and no decoration. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `dist/projects/index.html` exists and contains project card markup
3. Both sample projects render with all fields visible
4. Tech stack badges display for each project
5. GitHub link appears on both cards; Visit link only on the first
</verification>

<success_criteria>
- /projects displays project cards in a responsive grid (1 col mobile, 2 cols desktop)
- Each card shows title, description, tech stack badges, and external links
- Optional fields (url) gracefully omit when absent
- Brutalist card styling: hard borders, no rounded corners, high contrast
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-02-SUMMARY.md`
</output>
