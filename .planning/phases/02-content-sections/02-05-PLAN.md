---
phase: 02-content-sections
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - src/pages/blog/[slug].astro
autonomous: true
requirements: [BLOG-03, BLOG-06]
gap_closure: true

must_haves:
  truths:
    - "BLOG-03 requirement text matches the actual blog schema fields (title, date, description, tags — no slug)"
    - "Every blog post og:image meta tag resolves to an existing file (/og-image.png)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected BLOG-03 description without slug"
      contains: "title, date, description, tags"
    - path: "src/pages/blog/[slug].astro"
      provides: "Blog post page without broken ogImage override"
  key_links:
    - from: "src/pages/blog/[slug].astro"
      to: "src/layouts/BaseLayout.astro"
      via: "ogImage prop defaults to /og-image.png"
      pattern: "BaseLayout.*title.*description"
    - from: "src/layouts/BaseLayout.astro"
      to: "public/og-image.png"
      via: "og:image meta tag default"
      pattern: "og:image.*ogImage"
---

<objective>
Close two verification gaps from 02-VERIFICATION.md: (1) update stale BLOG-03 requirement text that references a nonexistent "slug" field, and (2) fix broken per-post og:image by removing the ogImage override from [slug].astro so posts use the working shared fallback.

Purpose: The verification found 16/18 truths passing. These two fixes bring the phase to 18/18 — one documentation correction and one functional fix for broken OG image URLs.
Output: Updated REQUIREMENTS.md and [slug].astro with correct behavior.
</objective>

<execution_context>
@/home/parkermutsuz/.claude/get-shit-done/workflows/execute-plan.md
@/home/parkermutsuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-content-sections/02-VERIFICATION.md
@.planning/phases/02-content-sections/02-01-SUMMARY.md

<interfaces>
<!-- BaseLayout ogImage prop interface — executor needs to know the default behavior -->

From src/layouts/BaseLayout.astro:
```astro
interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const { title, description, ogImage = '/og-image.png' } = Astro.props;
```

When ogImage is NOT passed, BaseLayout defaults to `/og-image.png` which exists at `public/og-image.png`. The fix is to stop passing ogImage from [slug].astro so this default takes effect.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix broken og:image and update stale requirement</name>
  <files>src/pages/blog/[slug].astro, .planning/REQUIREMENTS.md</files>
  <action>
Two changes:

1. In `src/pages/blog/[slug].astro`, remove the `ogImage` prop from the BaseLayout component call on line 20. The line currently reads:
   `ogImage={`/og/${post.id}.png`}`
   This references nonexistent per-post images. Remove this prop entirely so BaseLayout falls back to its default `/og-image.png`. The resulting BaseLayout call should only pass `title` and `description`:
   ```
   <BaseLayout
     title={post.data.title}
     description={post.data.description}
   >
   ```
   Per-post OG image generation is a v2 concern (BLOG-07 covers Satori-based dynamic generation).

2. In `.planning/REQUIREMENTS.md`, update BLOG-03 on line 29 from:
   `- [ ] **BLOG-03**: Blog post frontmatter: title, date, description, tags, slug`
   to:
   `- [ ] **BLOG-03**: Blog post frontmatter: title, date, description, tags (Astro 5 uses post.id for URL routing — no slug field)`

Do NOT change any other lines in either file.
  </action>
  <verify>
    <automated>cd /home/parkermutsuz/dev/blog && npm run build && grep -c 'ogImage' src/pages/blog/\[slug\].astro | grep -q '^0$' && grep 'BLOG-03' .planning/REQUIREMENTS.md | grep -qv 'slug,'</automated>
  </verify>
  <done>
    - `src/pages/blog/[slug].astro` no longer passes ogImage prop to BaseLayout
    - Built blog post HTML at `dist/blog/sample-post/index.html` contains `og:image` pointing to `/og-image.png` (the working fallback)
    - REQUIREMENTS.md BLOG-03 says "title, date, description, tags" without "slug" as a listed field
    - `npm run build` passes without errors
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep 'og:image' dist/blog/sample-post/index.html` shows `/og-image.png` (not `/og/sample-post.png`)
3. `grep 'ogImage' src/pages/blog/\[slug\].astro` returns no matches
4. `grep 'BLOG-03' .planning/REQUIREMENTS.md` shows updated text without "slug" as a frontmatter field
</verification>

<success_criteria>
- Blog post og:image meta tag resolves to the existing shared fallback `/og-image.png`
- BLOG-03 requirement text accurately reflects the actual schema fields
- Build passes clean with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-05-SUMMARY.md`
</output>
